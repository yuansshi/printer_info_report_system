server {
    listen 127.0.0.1:8080;
    server_name _;
    root /var/www/printer-dashboard;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /data.js {
        add_header Cache-Control "no-store" always;
    }
}
